<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Show Code</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">
    </head>
    <body>
        <div class = "has-text-centered">
            <section class="hero is-primary">
                <div class="hero-body">
                    <div class="container">
                        <h1 class="title">
                            Details
                        </h1>
                    </div>
                </div>
            </section>
            <div class = "container">
                <p>
                    <strong>Title:</strong>
                    <%= @article.title %>
                </p>
                
                <p>
                <strong>Text:</strong>
                    <%= @article.text %>
                </p>
                <strong>Text:</strong>
                    <%= @article.id %>
                </p>
                
                <!--show text within file-->
                
                <%= link_to "View File", @article.file%>
                <br>
                
                <!--cdata_section(File.read(@filename)) %>-->
                <br>
                
                <!-- render file: "#{Rails.root}/storage/1j/wc/1jwcp2wpkwgdsmlnki80m6mjdq1y", layout: false %>-->
                
                Article_Path: <%= @article.file %>
                
                <pre><%= @text %></pre>

                <br>
                <button class="button is-info is-outlined">
                    <%= link_to "Back", articles_path %>
                </button>
            </div>
        </div>
    </body>
</html>